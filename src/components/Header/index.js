import React, {PureComponent} from 'react';
import './style.css';

class Header extends PureComponent {
  render() {

    return (
      <header>
        <h1>Layout testpage</h1>
      </header>
    );
  }
}

export default Header;
